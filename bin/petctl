#!/usr/bin/env bash
# Compatibility wrapper — the real implementation is petctl.mjs (cross-platform,
# zero dependencies). Kept so existing docs and muscle memory keep working.
exec node "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/petctl.mjs" "$@"
